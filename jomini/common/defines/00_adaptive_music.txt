NAdaptiveMusic = {
	MAX_PAUSE_FACTOR = 100.0					# Maximum accumulated music pause factor before it gets reset
	PAUSE_FACTOR_GAIN_SPEED = 0.5				# How much does the accumulated music pause factor grow per second during silence
	PAUSE_FACTOR_SILENCE_THRESHOLD = 10.0		# No mood tracks are played if the accumulated music pause factor is below this value
}
